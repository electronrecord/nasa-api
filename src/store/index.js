import { createStore } from 'vuex'
import * as projects from './modules/projects'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    projects
  }
})
